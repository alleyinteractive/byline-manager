<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="byline-manager" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">
  <description>PHP_CodeSniffer standard for byline-manager.</description>

  <!-- Include Alley Rules -->
  <rule ref="Alley-Interactive" />

  <!--
  Pass some flags to PHPCS:
    p flag: Show progress of the run.
    s flag: Show sniff codes in all reports.
    n flag: Do not print warnings.
  -->
  <arg value="ps" />

  <!-- Whenever possible, cache the scan results and re-use those for unchanged files on the next scan. -->
  <arg name="cache" value=".phpcs/cache.json" />

  <!-- Check up to 20 files simultaneously. -->
  <arg name="parallel" value="20" />

  <!-- Set severity to 1 to see everything that isn't effectively turned off. -->
  <arg name="severity" value="1" />

  <!-- The version set here matches the minimum version tested. -->
  <config name="minimum_supported_wp_version" value="5.9" />

  <!-- Check for cross-version support for PHP 8.0 and higher. -->
	<config name="testVersion" value="8.0-"/>

  <!-- Exclude a few directories and autogenerated files. -->
  <exclude-pattern>tests/</exclude-pattern>
  <exclude-pattern>build/</exclude-pattern>
  <exclude-pattern>vendor/</exclude-pattern>

  <!-- Define the prefixes that can be used by the plugin -->
  <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
    <properties>
      <property name="prefixes" type="array">
        <element value="byline_manager" />
        <element value="bylines" />
      </property>
    </properties>
  </rule>

  <rule ref="WordPress.Security.EscapeOutput">
    <properties>
        <property name="customAutoEscapedFunctions" type="array" value="get_the_byline,get_the_byline_posts_links,get_the_byline_links" />
    </properties>
  </rule>
</ruleset>
